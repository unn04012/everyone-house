# 공공임대 알리미 (Public Rental Notifier) — 프로젝트 스펙

> 상태 표기: ✅ 실측/결정 완료 · 🔧 빌드 시 확인 필요(TODO) · 🧩 설계 필요
> 코드 구조·컨벤션은 [CLAUDE.md](CLAUDE.md) 참조.

---

## 1. 목표

공공임대 **모집공고가 뜨면 → 내 소득·자산 기준을 대입해 → 적합한 공고만 알림**으로 받는다.
개인용 도구(personal tool). 차별점은 "소득 기반 개인 맞춤 필터" — 기존 공사 알리미엔 없는 부분.

## 2. 스코프 (좁힘 완료)

- **지역**: 서울 / 경기 위주
- **유형**: 청년·신혼부부 특화 위주 (행복주택, 청년안심주택, 매입임대, 신혼희망타운, 통합공공임대 등)
- **알림 빈도**: 하루 1~2회면 충분 (실시간 불필요)

→ 결론: **실시간 인프라 불필요, 헤드리스 브라우저 불필요** (아래 근거).

---

## 3. 데이터 소스 (실측 완료)

세 소스 조합. **핵심 목록이 전부 서버 렌더링(정적)** 이라 `fetch` + HTML 파서로 충분.
원본 공공기관 공식 공고를 직접 수집 (aggregator 크롤링은 지양 — 영리 중개·SPA·약관 리스크).

> 🔒 **엔드포인트와 도출 절차는 [docs/sources.local.md](docs/sources.local.md) 에 있다 (git 제외).**
> 공개 저장소에 수집 경로를 남기지 않기 위해 분리했으며, 실행 시 `.env` 의 `SH_*` / `MYHOME_*` 로 주입된다.

### 3.1 마이홈포털 모집공고 API — 베이스 레이어 ✅
- data.go.kr 서비스 **15108420** (국토부 마이홈포털 공공주택 모집공고 조회). 무료 JSON REST.
- LH 물량이 대부분이라 SH 와 거의 겹치지 않는다 → 커버리지가 실질적으로 늘어난다.
- 지역·유형 필터 지원, 모집기간(`beginDe`/`endDe`)까지 제공 → **D-day 계산 가능**.
- ⚠️ "현재 모집중" 스냅샷만 반환. ⚠️ 같은 공고가 단지별로 중복 → `pblancId` 로 묶어야 함.

### 3.2 SH — 서울주거포털 (목록) ✅ 정적
- 완전 서버 렌더링, GET 페이지네이션, 한 페이지 10건.
- 컬럼: 번호 / 청약유형 / 공고명 / 공고게시일 / 발표일 / 모집상태 / 담당부서 / 상세링크
- ⚠️ 모집상태가 빈 칸인 행이 존재 → UNKNOWN 으로 정규화.
- ⚠️ 목록에 **접수 마감일이 없다** → D-day 계산 불가. 상세/공고문에서 보완 필요.

### 3.3 공고문 PDF ✅ (SH·LH 모두 확보)
- SH: 미리보기 리다이렉트를 역산해 원본 파일 URL 을 얻는다 (GET).
- LH: 마이홈 상세의 다운로드 파라미터로 받는다 (POST 전용 → GET URL 없음).
- **디지털 PDF 라 OCR 이 불필요하다** — 텍스트 추출이 깨끗하다. SPEC §7 의 "PDF 파싱은 후순위" 전제는 폐기.

### 3.4 GH — apply.gh.or.kr 🔧 미착수
- 1페이지는 서버 렌더링. 페이지네이션·상세는 내부 POST → 파라미터 1회 확인 필요.
- 신규 감지는 1페이지 GET 으로 충분(새 공고는 맨 위).

## 4. 아키텍처 결정 ✅

- **헤드리스 브라우저 불필요** (전 소스 핵심 목록 정적).
- 크롤러 = `fetch` + HTML 파서(cheerio / node-html-parser) + 마이홈 JSON 호출.
- **MVP는 Render 단일 플랫폼**:
  | 조각 | Render 서비스 |
  |---|---|
  | 크롤러 (하루 1~2회) | Cron Job |
  | API 서버 | Web Service |
  | 프론트 | Static Site |
  | DB | PostgreSQL (또는 Neon/Supabase — §6) |
  | (옵션) 캐시/큐 | Key Value (Redis 호환) |
- 파이프라인: `스케줄 → 수집 → 정규화 → dedupe(기존 seen 비교) → 자격 필터 → 신규 적합분 알림`
- 알림 채널(MVP): 이메일 또는 텔레그램. (카톡 알림톡은 사업자 온보딩 필요 → 후순위)
- 크롤러는 **나중에 Lambda/서버리스로 이관할 수 있게** 별개 배포 단위로 유지한다 → 구조·불변조건은 CLAUDE.md 참조.

## 5. 이식성 / 향후 이관 ✅ 방침

목표: 이관을 "코드 재작성"이 아니라 "설정 변경"으로 만든다.

- 처음부터 **Dockerfile 배포** (Render도 지원) → 나중에 EC2/ECS/Lambda 컨테이너에서 동일 이미지 실행.
- **ORM + 마이그레이션 툴**(Drizzle) → 스키마 버전관리, 어디서든 `migrate`로 재현.
- DB 접속은 **`DATABASE_URL` 환경변수로만**.
- 12-factor 준수(설정=env, 상태=DB, 컨테이너=무상태).
- EC2 이관 순서: ① 같은 이미지 EC2에서 실행 → ② `DATABASE_URL` 유지한 채 컴퓨트만 먼저 이전·안정화 → ③ 이후 `pg_dump | pg_restore`로 DB 이전.
- Lambda 이관 시 복병은 **Postgres 커넥션 고갈** → DB 클라이언트 팩토리를 드라이버 교체 가능하게 유지.
- ⚠️ 프로덕션 DB를 EC2 위 Docker 컨테이너에 직접 띄우지 말 것 → **RDS 권장**(셀프호스팅 시 EBS 볼륨 + 스냅샷 백업 필수).

## 6. 비용 메모 (Render, 2026 기준)

- 무료 Web Service: 15분 무트래픽 시 슬립 + ~1분 콜드스타트, 워크스페이스당 750h/월.
- **무료 Postgres: 생성 30일 후 삭제(14일 유예), 1GB 캡, 프로덕션 부적합** → 지속 서비스엔 유료(Basic ~$6/월) 또는 외부 무료 Postgres(Neon/Supabase).
- Cron Job: 실행 시간 분당 과금, 크론당 월 최소 $1(하루 몇 분이면 사실상 바닥값).
- 상시 Starter Web + Basic PG ≈ 월 ~$13.
- 팁: Postgres를 Neon/Supabase로 빼면 30일 만료 회피 + Postgres 유지라 이관도 여전히 무통증. Lambda 이관 계획이 있으면 처음부터 이쪽이 유리.

---

## 7. 자격 판정 엔진 — 핵심 난제 🧩

**완전 자동 확정은 불가.** "자동 1차 필터"로 설계한다.

- 유형별 소득 기준이 다름:
  - **통합공공임대**: 기준 중위소득 % 기준
  - **국민임대 / 행복주택 등**: 전년도 도시근로자 가구원수별 월평균소득 % (70/100/120/150 …)
- 소득 외에 **총자산 · 자동차가액 · 무주택 · 나이 · 혼인 · 지역 거주요건**까지 봐야 최종 자격.
  - 예시(GH): 청년 총자산 ≤ 2억 7,300만, 신혼 등 ≤ 3억 4,500만, 차량 ≤ 3,708만 (연도별 갱신).
- 상세 자격이 공고문 PDF에만 있는 경우 多 → 그래서 3단계 판정:
  **"적합 가능성 높음 / 검토 필요 / 미해당"** (딱 잘라 yes/no 하지 않음).
- 설계:
  1. 사용자 입력: 가구원수 / 월소득 / 총자산 / 자동차가액 / 무주택 여부 / 지역 / 나이 / 혼인상태
  2. 규칙엔진: `공고 유형 → 기준표 → 가구원수별 금액 환산 → 비교`
  3. 기준표는 **연 1회 갱신되는 코드 상수 테이블로 분리** 관리(도시근로자 월평균소득표, 기준 중위소득표).
- 🔧 TODO: 최신 연도 도시근로자 가구원수별 월평균소득 표 + 기준 중위소득 표 확보.

## 8. 데이터 모델 (초안 🧩)

```
sources        # 소스 정의 (myhome_api | sh_portal | gh_apply ...)
notices        # 정규화된 공고 (source_id, external_id, 유형, 지역, 제목,
               #   게시일, 마감일, 상태, 상세URL, PDF_URL, raw_json)
seen_index     # dedupe용 (source_id + external_id 유니크)
user_profile   # 가구원수/소득/자산/무주택/지역/나이/혼인 등
matches        # notice_id + profile 판정결과(적합/검토/미해당 + 사유 + ruleset 버전)
notifications  # 발송 로그 (match_id, 채널, 발송시각, 상태)
```

> `eligibility_rules` 테이블은 두지 않는다 — 기준표는 코드 상수(§7, 결정 로그 D3).

## 9. 준법 / 예의 ✅

- 원본 공공기관 공식 공고 직접 수집 (aggregator 지양).
- robots.txt 확인, 하루 1~2회 저빈도, User-Agent 명시, 실패 시 지수 백오프.
- 개인 용도.

---

## 10. 다음 작업 순서

- [ ] 🧩 모노레포 스캐폴딩 (npm workspaces + Nest + Drizzle) — 구조는 CLAUDE.md
- [ ] 🔧 GH POST 파라미터 확인 (페이지네이션 / 상세 / 첨부 다운로드)
- [ ] 🔧 data.go.kr 마이홈 API 키 발급 + 응답 필드 확정
- [ ] 🧩 소득·자산 기준표 정리 (§7) — 최신 연도 수치 확보
- [ ] 🧩 DB 스키마 확정 + 마이그레이션 셋업 (Drizzle)
- [ ] 크롤러 3종 구현 → 공통 정규화 스키마로 매핑
- [ ] dedupe + 자격 필터(3단계) 로직
- [ ] 알림 채널(이메일/텔레그램) 연동
- [ ] 프론트: 프로필 입력 + 공고 목록/적합도 대시보드
- [ ] Dockerfile + Render 배포(Web/Cron/Static/PG) + `DATABASE_URL` env

---

## 결정 로그

| # | 결정 | 근거 |
|---|---|---|
| D1 | 모노레포 (`apps/api`, `apps/crawler`, `apps/web` + `packages/domain`, `packages/db`) | 배포 단위 3개가 도메인 코드를 공유. 워크스페이스는 나중에 붙이는 비용이 훨씬 크다. |
| D2 | 크롤러를 API와 별개 앱으로 분리 | Lambda 이관 옵션 유지. 폴더 경계가 결합을 빌드 단계에서 차단. |
| D3 | 기준표는 코드 상수, `eligibility_rules` DB 테이블 폐기 | 변경 주기(연 1회) = 배포 주기. git 히스토리가 감사 로그. PR에서 숫자 리뷰 가능. |
| D4 | 판정 엔진은 `packages/domain`(순수) | crawler·api 양쪽이 필요(프로필 수정 시 재판정). 순수성을 빌드가 강제. |
| D5 | 판정 결과에 ruleset 버전 스탬프 | 과거 판정 사유 추적의 유일한 수단. |
| D6 | NestJS + DDD, lotto-automation 컨벤션 승계 | 기존 자산 재사용. 단 툴체인 제약으로 일부 이탈(D7~D9). |
| D7 | 전 스택 최신: Node 26.9 / Nest 12.0.4 / TS 7.0.2 / zod 4 | 실제 설치·빌드·실행·테스트로 조합 검증 (2026-09-21). |
| D8 | **ESM** (`type: module`, `module: nodenext`) | Nest 12가 ESM 전용 패키지. CJS로 두면 영구적으로 `require(esm)` interop에 의존하고, 의존성이 top-level await을 쓰면 깨진다. |
| D9 | 빌드 `tsc` (`nest build` 아님), 테스트 `@swc/jest` (`ts-jest` 아님) | `@nestjs/cli@12`는 TS 6 고정, `ts-jest`는 peer가 TS 7 배제. 상세는 CLAUDE.md 「툴체인 제약」. |
